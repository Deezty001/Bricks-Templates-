version: '3.8'

services:
  wordpress:
    image: wordpress:latest
    ports:
      - "8081:80"
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: wpuser
      WORDPRESS_DB_PASSWORD: securepassword123
      WORDPRESS_DB_NAME: render_db
    volumes:
      - wp_data:/var/www/html
      # This automatically mounts our custom plugin so you don't have to upload it!
      - ./bricks-renderer.php:/var/www/html/wp-content/plugins/bricks-renderer/bricks-renderer.php
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: mariadb:10.5
    environment:
      MYSQL_ROOT_PASSWORD: secure_root_password
      MYSQL_DATABASE: render_db
      MYSQL_USER: wpuser
      MYSQL_PASSWORD: securepassword123
    volumes:
      - db_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  wp_data:
  db_data:
